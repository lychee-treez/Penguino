<% provide(:title, @user.name)%>

<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <%= image_tag @user.image.url, :class => "prof_pic" %>
      <h1>
      <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
